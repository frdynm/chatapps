import 'package:flutter/material.dart';

class ErorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Eror Page"),
    );
  }
}
